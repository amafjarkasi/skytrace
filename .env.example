# Copy to .env and fill in. Never commit .env.
# Used once to download Universe weights for --backend local (then inference is on-device).
# Also used for --backend roboflow (cloud HTTP — costs credits per frame).
ROBOFLOW_API_KEY=
